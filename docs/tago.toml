title       = "fb"
baseURL     = "https://facebook-cli.tamnd.com/"
description = "Facebook as structured data, from one pure-Go binary. fb reads the Relay store the site ships to a signed-out browser and turns pages, posts, comments, media, groups and events into records and a claim graph you can query and export as RDF. No app id, no developer app, no API token."
contentDir  = "content"
outputDir   = "public"
theme       = "tago-doks"
syntaxHighlight = "true"

[params]
github = "https://github.com/tamnd/facebook-cli"
